name: Linux

on:
  push:
    branches: [main]
    paths:
      - '**'
      - '!stats.txt'
      - '!README.md'

  pull_request:
    paths:
      - '**'

  release:
    types: [published]

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    permissions:
          contents: write

    strategy:
      matrix:
        os: [ubuntu-22.04]
        qt_ver: [6.6.3]
        qt_arch: [gcc_64]

    env:
        targetName: Knot
        VERSION: Linux
        orgName: Knot
        ONEDRIVE_SECRET: ${{ secrets.ONEDRIVE_SECRET }}

        QT_VERSION: 6.6.3
        QT_ARCH: gcc_64
        # ✅ 修复：环境变量变量名错误，必须加${}，之前是纯字符串，路径无效
        QT_INSTALL_DIR: /opt/qt/${{ matrix.qt_ver }}/gcc_64
        QT_INSTALL_QML: /opt/qt/${{ matrix.qt_ver }}/gcc_64/qml
        QML2_IMPORT_PATH: ${{ github.workspace }}/qml:/opt/qt/${{ matrix.qt_ver }}/gcc_64/qml

    steps:

      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: ${{ matrix.qt_ver }}
          cached: 'false'
          modules: qtcharts qtsensors qtwebview qtlocation qtwebchannel qt5compat qtpositioning qtmultimedia

      - name: ubuntu install all dependencies (IBus+fcitx5+基础库)
        run: |
         sudo apt-get update -y && sudo apt-get upgrade -y
         sudo apt-get install -y libglew-dev libglfw3-dev libxcb*
         sudo apt-get install -y libxkbcommon-dev
         sudo add-apt-repository universe -y
         sudo apt install -y libfuse2
         sudo apt-get install -y libsecret-1-dev
         sudo apt-get install -y libqt6serialport6
         # ✅ IBus输入法核心依赖（无报错版本）
         sudo apt-get install -y libibus-1.0-dev ibus libqt6gui6 libglib2.0-dev

      - uses: actions/checkout@v4
        with:
                  fetch-depth: 1

      - name: build ubuntu
        run: |
                  qmake
                  make -j$(nproc)

      - name: install linuxdeployqt
        run: |
          wget -c -nv "https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage"
          chmod a+x linuxdeployqt-continuous-x86_64.AppImage

      - name: Remove unused SQL drivers (SAFE MODE)
        run: |
            export PATH="${{ env.QT_DIR }}/bin:$PATH"
            QT_PLUGINS_DIR=$(qmake -query QT_INSTALL_PLUGINS)
            mkdir -p /tmp/qt-drivers-backup
            cp "$QT_PLUGINS_DIR/sqldrivers/"* /tmp/qt-drivers-backup/
            find "$QT_PLUGINS_DIR/sqldrivers" \
              -name "libqsql*.so" \
              ! -name "*sqlite*" \
              ! -name "*SQLite*" \
              -delete
            ls -l "$QT_PLUGINS_DIR/sqldrivers"

      # ✅ 核心修复区：打包环节 全量修复，解决所有问题
      - name: package (✅ FIX ALL BUGS: 权限+插件+路径+输入法)
        run: |
         cp res/icon.png bin/release/icon.png
         cp res/default.desktop bin/release/default.desktop
         cp ExtBin/linux/AppRun bin/release/AppRun
         chmod +x bin/release/AppRun

         # 应用根目录定义
         APP_ROOT="bin/release"
         APP_LIB_DIR="${APP_ROOT}/lib"
         APP_PLUGIN_ROOT="${APP_ROOT}/plugins"
         # ✅ 关键1：所有插件/库 都放在【应用自身目录下】，绝对不碰系统目录，彻底规避权限问题
         APP_INPUT_PLUGIN_DIR="${APP_PLUGIN_ROOT}/platforminputcontexts"
         mkdir -p "${APP_LIB_DIR}"
         mkdir -p "${APP_INPUT_PLUGIN_DIR}"
         # 给应用目录赋权，防止后续写入权限不足
         chmod -R 777 "${APP_ROOT}"

         # ========== 保留：fcitx5输入法支持 完整配置 ==========
         cp ExtBin/linux/fcitx5/libFcitx5Qt6DBusAddons.so.5.1.9 "${APP_LIB_DIR}/"
         cp ExtBin/linux/fcitx5/libfcitx5platforminputcontextplugin.so "${APP_INPUT_PLUGIN_DIR}/"
         # 创建软链接
         (cd "${APP_LIB_DIR}" && \
         ln -sf libFcitx5Qt6DBusAddons.so.5.1.9 libFcitx5Qt6DBusAddons.so.5 && \
         ln -sf libFcitx5Qt6DBusAddons.so.5 libFcitx5Qt6DBusAddons.so)

         # ========== 保留+优化：IBus输入法完整支持 ==========
         # 拷贝IBus运行依赖库到应用目录
         cp /usr/lib/x86_64-linux-gnu/libibus-1.0.so.5 "${APP_LIB_DIR}/"
         ln -sf "${APP_LIB_DIR}/libibus-1.0.so.5" "${APP_LIB_DIR}/libibus-1.0.so"
         # ✅ 关键优化：IBus插件直接用【Qt6自带的】，路径绝对正确，无拷贝失败
         cp "${{ env.QT_DIR }}/plugins/platforminputcontexts/libibusplatforminputcontextplugin.so" "${APP_INPUT_PLUGIN_DIR}/"

         # ========== 插件目录权限修复 ==========
         chmod -R 755 "${APP_PLUGIN_ROOT}"
         chmod 755 "${APP_LIB_DIR}/libibus-1.0.so.5"

         # ========== 拷贝其他依赖 ==========
         cp -r ExtBin/linux/nss/* "${APP_LIB_DIR}/"
         cp -P /usr/lib/x86_64-linux-gnu/libssl.so.3* "${APP_LIB_DIR}/"

         # ========== ✅ 关键2：设置环境变量，让linuxdeployqt只识别【应用自身插件】，不找系统插件
         export QT_PLUGIN_PATH="${APP_PLUGIN_ROOT}:${{ env.QT_DIR }}/plugins"
         export LD_LIBRARY_PATH="${APP_LIB_DIR}:${APP_INPUT_PLUGIN_DIR}:${{ env.QT_DIR }}/lib:$LD_LIBRARY_PATH"
         export QML2_IMPORT_PATH="${{ github.workspace }}/qml:${{ env.QT_DIR }}/qml"
         export VERSION

         # ========== ✅ 终极核心修复：linuxdeployqt 关键参数，彻底解决 /plugins Permission denied 错误
         # 核心参数说明：
         # 1. -bundle-non-qt-libs ：将所有依赖库打包到应用内部，不写系统目录
         # 2. -no-copy-copyright-files ：禁止写入系统版权目录，规避权限
         # 3. -exclude-libs=libQt6* ：禁止重复拷贝Qt库，防止路径冲突
         # 4. 去掉-extra-plugins=platforminputcontexts ：插件已在应用目录，无需linuxdeployqt拷贝，这是根目录写入的元凶！
         # 5. -appimage-extract-and-run ：AppImage解压运行，规避AppImage自身权限问题
         ./linuxdeployqt-continuous-x86_64.AppImage ${APP_ROOT}/${targetName} \
                  -unsupported-allow-new-glibc \
                  -qmldir=${{ github.workspace }}/Knot/src/qmlsrc \
                  -qmake=${{ env.QT_DIR }}/bin/qmake \
                  -appimage \
                  -appimage-extract-and-run \
                  -bundle-non-qt-libs \
                  -no-copy-copyright-files \
                  -exclude-libs=libQt6Core.so.6,libQt6Gui.so.6,libQt6Qml.so.6

         # 给最终产物赋权
         chmod +x ${targetName}-${VERSION}-x86_64.AppImage

      # 上传artifacts
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ env.orgName }}-${{ env.VERSION }}-x86_64.AppImage
          path: ${{ env.targetName }}-${{ env.VERSION }}-x86_64.AppImage

     # tag 上传Release
      - name: uploadRelease
        if: github.event_name == 'release'
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ${{ env.targetName }}-${{ env.VERSION }}-x86_64.AppImage
          asset_name: ${{ env.orgName }}-${{ env.VERSION }}-x86_64.AppImage
          tag: ${{ github.ref }}
          overwrite: true
